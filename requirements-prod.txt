# وابستگی‌های فقط-تولید (میزبانی/هاست/Docker)
# نصب:  pip install -r requirements.txt -r requirements-prod.txt
-r requirements.txt

# WSGI سرور برای production — جایگزین Flask dev server
gunicorn==23.0.0
