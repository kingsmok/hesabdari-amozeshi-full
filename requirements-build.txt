# ابزارهای لازم فقط برای ساخت نسخه‌ی ویندوزی (روی سرور/هاست نصب نمی‌شوند)
# نصب:  pip install -r requirements.txt -r requirements-build.txt
-r requirements.txt

pyinstaller>=6.0
PyQt6>=6.6
PyQt6-WebEngine>=6.6
