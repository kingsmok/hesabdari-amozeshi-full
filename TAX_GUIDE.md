# راهنمای ماژول‌های مالیاتی

این سند کاربرد و راه‌اندازی امکانات انطباق مالیاتی افزوده‌شده به نرم‌افزار را توضیح می‌دهد.

---

## ۱. اتصال به سامانه مودیان

مسیر: **سایدبار → مالیات و مودیان → داشبورد مودیان**

### راه‌اندازی اولیه
1. به `تنظیمات مالیاتی` بروید.
2. بخش «هویت مودی» را کامل کنید (شناسه ملی/کد ملی، کد اقتصادی، کد پستی، شماره ثبت).
   شناسه فروشنده هنگام ذخیره **الگوریتمی اعتبارسنجی** می‌شود.
3. `شناسه یکتای حافظه مالیاتی` (۶ کاراکتری) دریافتی از کارپوشه را وارد کنید.
   حروف `I J L Q V` طبق سند سازمان ممنوع هستند و فرم آن‌ها را رد می‌کند.
4. مسیر فایل کلید خصوصی (PEM) و گواهی امضا را وارد کنید.
5. با دکمه `تست اتصال` صحت پیکربندی را بررسی کنید.

### حالت آزمایشی (Sandbox)
تا زمانی که گواهی امضای الکترونیک ندارید، گزینه **«حالت آزمایشی»** را روشن بگذارید:
- شماره منحصر به فرد مالیاتی **واقعی** تولید و ذخیره می‌شود.
- ارسال شبیه‌سازی می‌شود و هیچ درخواستی به سرور سازمان نمی‌رود.
- کل جریان صدور → ارسال → استعلام قابل تمرین است.

پس از دریافت گواهی، این گزینه را خاموش کنید تا ارسال واقعی انجام شود.

### جریان فنی ارسال (API v2)
```
GET  /nonce?timeToLive=30              → دریافت nonce
JWS  RS256 + x5c + sigT  از {nonce, clientId}   → توکن Bearer
GET  /server-information               → کلید عمومی سرور
JWS  امضای JSON صورتحساب
JWE  RSA-OAEP-256 / A256GCM
POST /invoice   [{payload, header:{requestTraceId, fiscalId}}]
GET  /inquiry-by-reference-id          → استعلام وضعیت
```
تمام تعاملات در **لاگ ارسال** با کد HTTP، Trace ID و پیام سازمان ثبت می‌شوند.

---

## ۲. شماره منحصر به فرد مالیاتی

ساختار ثابت ۲۲ کاراکتری مطابق سند «قالب شناسه یکتای حافظه مالیاتی»:

| بخش | طول | توضیح |
|---|---|---|
| شناسه یکتای حافظه مالیاتی | ۶ | از کارپوشه |
| تاریخ ثبت صورتحساب | ۵ | Hex تعداد روز از 1970/01/01 |
| سریال داخلی صورتحساب | ۱۰ | Hex، تصاعدی |
| رقم کنترلی | ۱ | الگوریتم Verhoeff |

نمونه تاییدشده با سند سازمان:
```
DEF5GH + 0481F + 000000000C + 2  →  DEF5GH0481F000000000C2
```

صفحه `شماره مالیاتی` دو ابزار دارد: **تولید** و **اعتبارسنجی/تجزیه**.
سریال داخلی به‌صورت خودکار از تنظیمات (`last_serial`) افزایش می‌یابد و
هنگام «صدور» صورتحساب تخصیص داده می‌شود.

---

## ۳. الگوهای صورتحساب

هر هفت الگوی صورتحساب نوع اول پشتیبانی می‌شود:

| کد | الگو |
|---|---|
| ۱ | فروش کالا و خدمات |
| ۲ | فروش ارز |
| ۳ | طلا، جواهر و پلاتین |
| ۴ | قرارداد پیمانکاری |
| ۵ | قبوض خدماتی |
| ۶ | بلیط هواپیما |
| ۷ | صادرات |

در الگوی **صادرات** اطلاعات خریدار داخلی حذف و ارز/نرخ برابری در هدر درج می‌شود.
انواع صورتحساب (`inty`) و موضوع (`inp`: اصلی/اصلاحی/ابطالی/برگشت از فروش) نیز قابل انتخاب هستند.

---

## ۴. مالیات بر ارزش افزوده

- نرخ عمومی در `تنظیمات مالیاتی` تعریف می‌شود (پیش‌فرض ۱۰٪) و روی ردیف‌های
  صورتحساب خرید و فروش **به‌صورت خودکار** اعمال می‌گردد.
- در `کالا و خدمات` می‌توان برای هر قلم تیک **«معاف از ارزش افزوده»** زد
  (مثلاً خدمات آموزشی) و دلیل معافیت را ثبت کرد؛ روی این اقلام VAT محاسبه نمی‌شود.
- گزینه «خدمات آموزشی پیش‌فرض معاف» باعث می‌شود ردیف‌های جدید و صورتحساب‌های
  تولیدشده از پرداخت شهریه به‌طور خودکار معاف در نظر گرفته شوند.
- **گزارش ارزش افزوده** (فصلی) مابه‌التفاوت VAT فروش و خرید را برای اظهارنامه
  محاسبه و خروجی اکسل سه‌شیتی (خلاصه/فروش/خرید) تولید می‌کند.

---

## ۵. مالیات حقوق و دستمزد

- **پله‌های مالیاتی:** در صفحه `پله‌های مالیاتی` برای هر سال، سقف معافیت و پله‌های
  ۱۰٪/۱۵٪/۲۰٪/… (مبالغ ماهانه) قابل تعریف است. یک محاسبه‌گر نمونه هم کنار فرم قرار دارد.
- **محاسبه خودکار:** دکمه «بازمحاسبه مالیات دوره» مالیات همه فیش‌های آن دوره را
  بر مبنای پله‌های سال محاسبه و خالص پرداختی را به‌روزرسانی می‌کند.
- **خروجی لیست مالیات:** دو قالب برای بارگذاری در `salary.tax.gov.ir`:
  - **اکسل** با ۱۵ ستون استاندارد (نام، کد ملی، ناخالص، معافیت، بیمه، مشمول، مالیات، خالص …)
  - **فایل متنی** جداشده با `|` به همراه هدرهای `#PERIOD` و `#EMPLOYER`

صفحه هشدار می‌دهد اگر فردی **کد ملی ثبت‌شده ندارد**، چون سامانه ردیف بدون کد ملی را رد می‌کند.

---

## ۶. گزارش معاملات فصلی (ماده ۱۶۹)

- خروجی **اکسل دو شیتی (فروش/خرید)** و **CSV با BOM** سازگار با فرمت TTMS
  شامل ۱۷ ستون (نوع معامله، شناسه ملی، کد اقتصادی، کد پستی، نشانی، مبالغ، شماره مالیاتی).
- بازه هر فصل شمسی به‌صورت دقیق محاسبه می‌شود (بهار: ۱ فروردین تا ۳۱ خرداد و …).
- **کنترل صحت شناسه‌ها:** قبل از خروجی‌گیری، فهرست طرف‌های معامله با کد ملی/شناسه ملی
  نامعتبر نمایش داده می‌شود تا پیش از ارسال اصلاح شوند.

### اعتبارسنجی شناسه‌ها
| نوع | طول | الگوریتم |
|---|---|---|
| کد ملی اشخاص حقیقی | ۱۰ | وزن‌دهی ۱۰..۲ و رقم کنترلی مبنای ۱۱ |
| شناسه ملی اشخاص حقوقی | ۱۱ | ضرایب 29,27,23,19,17 با افزوده رقم دهم |
| کد فراگیر اتباع | ۱۲ | کنترل ساختاری |
| کد اقتصادی | ۱۲/۱۴ | کنترل ساختاری |

بررسی به‌صورت **زنده (AJAX)** هنگام تایپ در فرم طرف حساب و فرم مالیات تکلیفی انجام می‌شود.
دکمه «اعتبارسنجی مجدد همه» برای بازبینی انبوه پس از ورود داده وجود دارد.

---

## ۷. مالیات‌های تکلیفی

صفحه `مالیات تکلیفی` اسناد کسر و واریز را مدیریت می‌کند:
- **مالیات اجاره** (نرخ پیش‌فرض قابل تنظیم)
- **حق‌الزحمه ماده ۸۶** برای اساتید حق‌التدریس و مشاوران غیرکارمند (معمولاً ۱۰٪)
- قرارداد پیمانکاری و سایر

هر سند مبلغ ناخالص، نرخ، مالیات کسرشده و خالص پرداختی را نگه می‌دارد و وضعیت
«واریز شده/در انتظار» با شماره رهگیری قابل ثبت است. خروجی اکسل هم دارد.
با انتخاب یک مدرس، نام و کد ملی خودکار پر و نوع سند روی «ماده ۸۶» تنظیم می‌شود.

---

## ۸. گزارش‌های قانونی و اظهارنامه عملکرد

| گزارش | توضیح |
|---|---|
| **ترازنامه** | تفکیک دارایی/بدهی/سرمایه با کنترل توازن و نمایش اختلاف |
| **سود و زیان** | درآمدها، هزینه‌ها و هزینه استهلاک دوره |
| **دفتر روزنامه** | ریز اسناد تاییدشده سال مالی + هشدار اسناد نامتوازن |
| **دفتر کل** | جمع بدهکار/بستانکار و مانده هر حساب |

خروجی اکسل دفاتر (دو شیت روزنامه و کل) برای تحریر دفاتر پلمپ آماده است.
تمام گزارش‌ها فقط اسناد با وضعیت `confirmed` یا `approved` را در نظر می‌گیرند.

### استهلاک دارایی‌ها (ماده ۱۴۹)
- دو روش استاندارد: **خط مستقیم** و **نزولی**.
- جدول گروه‌های پیش‌فرض (ساختمان، تاسیسات، اثاثه، رایانه، تجهیزات آزمایشگاهی،
  وسایل نقلیه، نرم‌افزار) با عمر مفید و نرخ پیشنهادی؛ انتخاب گروه فیلدها را خودکار پر می‌کند.
- استهلاک هرگز ارزش دفتری را زیر **ارزش اسقاط** نمی‌برد.
- ثبت استهلاک به‌صورت تکی یا **گروهی برای همه دارایی‌ها** در یک سال مالی.
- جدول پیش‌بینی کامل عمر دارایی برای برنامه‌ریزی.

---

## راه‌اندازی و آزمون

```bash
pip install -r requirements.txt   # cryptography برای امضای JWS/JWE اضافه شده
python test_tax.py                # بررسی فنی ماژول‌های مالیاتی
python test_all.py                # بررسی سلامت کلی سیستم
```

`test_tax.py` هیچ درخواستی به سرور سازمان ارسال نمی‌کند، روی سال آزمایشی مجزا کار
می‌کند و داده‌ای را حذف نمی‌کند. نمونه‌های رسمی سند سازمان (`DEF5GH0481F000000000C2`)
به‌عنوان مورد آزمون در آن گنجانده شده‌اند.

> **نکته:** اگر `cryptography` نصب نباشد، برنامه از کار نمی‌افتد؛ فقط هنگام امضای
> واقعی پیام فارسی روشن با دستور نصب نمایش داده می‌شود و حالت آزمایشی کار می‌کند.
